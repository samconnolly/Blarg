<! The main page, showing all the pages >
{% extends "layout.html" %}
{% block body %}

<h1> Forums </h1>

{% for i in range(1,6) %}
<br>
<h2>Day {{i}} Forums</h2>
<form action="{{ url_for('show_entries',n=i) }}" >
<br><button type="submit" value=link name="link">Science Forum {{i}}</button>
</form>

<form action="{{ url_for('show_entries',n=(i+5)) }}" >
<button type="submit" value=link name="link">Media Forum {{i}}</button>
</form>

{% if i != active %}
<form action="{{ url_for('activate_forum') }}" method=post>
<button type="submit" value={{i}} name="activate">Activate Day {{i}} Forums</button>
</form>
{% else %}
Day {{i}} forums active <br>
{% endif %}

{% endfor %}
<br><h2>Deactivate All Forums</h2>
<form action="{{ url_for('activate_forum') }}" method=post>
<br><button type="submit" value=0 name="activate">Deactivate All Forums</button>
</form>

{% endblock %}



